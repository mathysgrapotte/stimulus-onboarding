.
We first split the dataset into a train and a validation set.

In Bio-DL, splitting at random is often not a good idea, most data is highly redundant, so it would induce data leakage.

Stimulus implements a wide array of splitting methods that you can use in your configuration.

{{italic:We also follow a principle of minimal abstraction, so you will often need to add your own, which is easy and does not require you to touch the codebase, but let's save this for another time.}}

You configure the splitting instructions like so: